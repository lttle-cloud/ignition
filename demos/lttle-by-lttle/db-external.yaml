# deploy this service only if you want to connect to the database from outside the cluster

service:
  name: db-external
  namespace: lbl
  target:
    name: db
    port: 5432
    protocol: tcp
  bind:
    external:
      host: lbl-db.eu.lttle.host
      protocol: tls
